# pyproject.toml + uv.lock are the manifest. This file is the pip-only
# fallback; it cannot express the two things the manifest does
# ([tool.uv] override-dependencies and the spikingjelly metadata correction),
# so the two lines that need them are spelled out below.
#
# Supported path:
#   make install              # uv sync --extra dev --extra loihi (+ patches)
#   make install SANAFE=1     # ... and the GPL-3.0 SANA-FE extra + its plugins

-e ../compilagent
-e .

# lava-nc 0.10.0 (the `loihi` extra) pins numpy<2, networkx<=2.8.7 and
# asteval<0.10, which torch 2.11 contradicts. --no-deps is the manual form of
# [tool.uv] override-dependencies:
#   pip install --no-deps lava-nc==0.10.0
#
# spikingjelly's pyproject at the pinned commit declares requires-python
# ">=3.11" and pulls torchaudio/tensorboard/SciencePlots/h5py, none of which
# this project needs. [[tool.uv.dependency-metadata]] states the truth for uv;
# pip needs it by hand:
#   pip install --no-deps --ignore-requires-python \
#     "spikingjelly @ git+https://github.com/fangwei123456/spikingjelly@fb71d12e211a1e80c14594aea72518743c90e5c3"
#
# The GPL-3.0 SANA-FE backend stays opt-in:
#   pip install sanafe==2.1.1 && python scripts/build_sanafe_plugins.py
