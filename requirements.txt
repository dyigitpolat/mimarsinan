nni
numpy
git+https://github.com/ildoonet/pytorch-gradual-warmup-lr.git
# [TS2] The Sobol' sampling backend seeds `qmc.Sobol` through the `rng=`
# argument scipy 1.15 renamed it to; on an older release the same call is a
# TypeError, so the floor is declared here rather than found at run time.
scipy>=1.15
# Pin the stable release: torch 2.12.0+cu130 was a bleeding-edge DEV build whose
# flash/mem-efficient SDP BACKWARD kernels SIGKILL the mapped-ViT attention during
# deployment training (offloaded_vit_deployment.md). 2.11.0 (same CUDA 13) has a
# working SDP backward and honors compilagent's torch>=2.11 floor.
torch==2.11.0
torchvision==0.26.0
einops
matplotlib
# [TS1] The NSGA-II budget stop rests on pymoo calling
# `termination.update(algorithm)` BEFORE the generation callback — behaviour,
# not API, verified against 0.6.1.6. A release that reorders it buys one extra
# generation past every budget boundary, so the bound is declared here rather
# than discovered by a red test.
pymoo>=0.6.1.6,<0.7
plotly
fastapi
uvicorn[standard]
websockets
pydantic-ai
python-dotenv
pytest>=8.0
pytest-xdist>=3.8
pytest-timeout>=2.4
pyflakes>=3.2.0
# lava-nc's learning_rule_applier imports asteval at graph-compile time.
asteval
# Compilagent is a hard dependency for the third architecture-search
# optimizer option (`arch_search.optimizer == "compilagent"`). Installed
# editable from the sibling repo so local edits propagate without a
# reinstall; release flow can swap to a pinned wheel.
-e ../compilagent