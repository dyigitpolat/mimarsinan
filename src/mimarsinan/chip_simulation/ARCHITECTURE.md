# chip_simulation/ -- Nevresim C++ Simulator Interface

Bridges Python and the nevresim C++ spiking neural network simulator.
Handles code generation, compilation, execution, and result parsing.
Also hosts optional Lava Loihi parity and SANA-FE detailed-stats backends.

## Key Components

| File | Symbols | Purpose |
|------|---------|---------|
| `nevresim/nevresim_driver.py` | `NevresimDriver` | Python-C++ bridge. Default **`connectivity_mode=runtime`** (`nevresim/connectivity.py`); override via config `nevresim_connectivity_mode`. Optional **`compile_cache_dir`**. |
| `nevresim/connectivity.py` | `DEFAULT_NEVRESIM_CONNECTIVITY_MODE`, `resolve_nevresim_connectivity_mode` | Deployment default and pipeline config resolution for chip wiring mode. |
| `nevresim/compile_nevresim.py` | `compile_simulator`, `CompileResult` | Compiles generated C++ with C++20; optional `-ftime-trace`, `-O0` |
| `nevresim/execute_nevresim.py` | `execute_simulator` | Runs binary; defaults to `cpu_count // 2` when `num_proc=0`. Drains stdout via `communicate()`, validates optional `expected_values`, surfaces stderr on failure. Protocol: one line per sample (`output_size` floats + newline). |
| `nevresim/segment_execute.py` | `run_binary_raw`, `save_segment_inputs` | SSOT for segment binary execution (input serialization, run, reshape). Used by `NevresimDriver` and hybrid precompiled path. |
| `nevresim/compile_cache.py` | `NevresimCompileCache` | Segment binary cache keyed by mapping + policy hash |
| `nevresim/profiling/` | `profile_mapping_compile`, `build_synthetic_mapping`, … | Synthetic compile sweeps and timing records (`scripts/profile_nevresim_compile.py`) |
| `code_generation/main_cpp_template_runtime.py` | Runtime main template | ``RuntimeChip`` uses **static** storage (not stack) to support large core counts |
| `simulation_runner/core.py` | `SimulationRunner` | End-to-end simulation for single-segment and multi-segment (hybrid) mappings. Reads `nevresim_connectivity_mode` from pipeline config. Parallel emit+compile capped at `cpu_count // 2`. |
| `hybrid_execution.py` | `assemble_segment_input_*`, `apply_input_shifts_numpy`, `execute_compute_op_*`, … | Shared segment I/O and compute ops for hybrid backends. `apply_input_shifts_numpy` is the numpy mirror of HCM's `_apply_input_shifts` (negative-value shift before the encode clamp); ComputeOp execution runs in the op module's own floating dtype. |
| `hybrid_semantics.py` | `NeuralSegmentResult`, `store_neural_segment_output`, `lif_inter_stage_from_spike_counts` | **Inter-stage contract**: LIF/rate → spike count / T; TTFS → activation in [0, 1]. All hybrid backends must use `store_neural_segment_output` after neural stages. `lif_inter_stage_from_spike_counts` is re-exported from the SSOT `mimarsinan.spiking.segment_boundary.decode_segment_output` (single source across sims). |
| `ttfs_executor.py` | `TtfsAnalyticalExecutor`, `run_ttfs_hybrid_contract` | Canonical TTFS segment semantics and shared hybrid contract runner (numpy float64 compute ops). Applies `node_output_shifts` (negative-value shift) to each assembled neural-stage input; under the **synchronized** `ttfs_cycle_based` schedule it then snaps the stage input to the single-spike timing grid (`ttfs_input_grid_quantize`) — the spike-time-encoded hardware boundary cannot carry off-grid values. Used by `record_ttfs_hcm_reference` and SANA-FE `contract_ttfs_*` fields. |
| `ttfs_encoding.py` | `ttfs_spike_time`, `ttfs_single_spike_train`, `ttfs_latched_spike_train`, `ttfs_input_grid_quantize` | TTFS input spike encoding (`round(S·(1−clamp(x)))`), spike-train builders, and the encode→decode round-trip `ttfs_input_grid_quantize`. The scalar kernels delegate to the `models/spiking/wire_semantics.py` numpy twins (one source for torch and numpy). |
| `ttfs_segment.py` | `segment_ttfs_arrays_from_mapping`, `run_ttfs_*`, `gather_segment_ttfs_output_from_cores` | Numpy TTFS execution and output gather. |
| `ttfs_recorder.py` | `TtfsRunRecord`, `compare_ttfs_contract_records`, `compare_ttfs_hardware_records` | TTFS activation parity records; contract vs hardware tolerance policies. |
| `neural_segment_executor.py` | `execute_neural_segment_analytical` | TTFS analytical dispatch helper for shared hybrid loop. |
| `hybrid_stage_runner.py` | `run_hybrid_stages`, `HybridStageContext` | Shared stage loop with `on_neural` / `on_compute` (and optional `after_neural` / `after_compute`). Used by **nevresim** (`SimulationRunner._run_hybrid`), **SANA-FE**, **Lava**, and **`SpikingHybridCoreFlow`** (HCM). |
| `firing_strategy.py` | `FiringStrategy`, `FiringStrategyFactory` | Reset + threshold validation; backend capability gates; `require_chip_faithful_lif_forward(cycle_accurate_lif_forward)` — Novena's zero-reset is arrival-order sensitive so it requires the cycle-accurate cascade forward (the analytical rate forward deploys unfaithfully); `DeploymentPlan.resolve` enforces it. See `mapping/FIRING.md`. |
| `behavior_config.py` | `NeuralBehaviorConfig` | Identity axes of the simulator-facing activation semantics (reset, comparison, spike encode). Used by Lava, SANA-FE, and codegen helpers; composed into `SpikingDeploymentContract`. |
| `deployment_contract.py` | `SpikingDeploymentContract` | **Cross-side deployment-semantics SSOT** (torch NF + simulators): composes `NeuralBehaviorConfig` and adds `simulation_steps`, `ttfs_cycle_schedule`, `encoding_layer_placement`, `bias_mode`. **Single-reader invariant**: `from_pipeline_config` is the only place these config keys are read (grep-guard test); everything downstream takes the contract. Derived getters (`is_synchronized`, `quantize_stage_input_to_grid`, `training_forward_kind`) answer schedule-derived questions so consumers stop re-deriving implications; all accept a reserved `core=None` kwarg for future per-core heterogeneity. `mode_policy()` returns the (firing × sync) `SpikingModePolicy` (V2) that carries `training_forward_kind` and the soma/calibration behavior. `calibration_pipeline(config, *, distmatch_driven, core)` (E3) resolves the conversion-health `CalibrationPipeline` for this (firing × sync) cell — the pipeline-wide axis every conversion tuner consumes (cascaded cycle opts in; LIF/analytical/synchronized get the inert pipeline ⇒ byte-identical). `conversion_policy(config, *, model, characterizer, context, core)` (E4) exposes the characterization-and-policy keystone (`ConversionPolicy.resolve`, propose → confirm → escalate) — DEFAULT-OFF: until `conversion_policy` is set the `ConversionDecision` names the CURRENT behavior (driver=controller, no characterization run) ⇒ byte-identical. **A6:** `context` (default None ⇒ byte-identical) is the trainer (the calibration-batch source) forwarded to the characterizer's `characterize`; the activated keystone uses `characterization.CascadeCharacterizer` (real forward-only probes) when enabled. |
| `spiking_mode_policy.py` | `SpikingModePolicy`, `LifModePolicy`, `TtfsAnalyticalModePolicy`, `TtfsSyncCycleModePolicy`, `TtfsCascadeModePolicy`, `policy_for_spiking_mode`, `ExecPolicySpec`, `NevresimExecParams` | **Behavior-carrying policy per (firing × sync)** (Vector V2). One polymorphic class per `(spiking_mode, schedule)` family carries `training_forward_kind()` (the NF the fine-tuners train through), `calibration_forward()` (the negative-shift NF forward), `soma_hw_name()` / `soma_model_attributes(...)` (the SANA-FE soma chain), `log_potential`, `decode_mode()` (the count-vs-timing decode `models/spiking/hybrid/flow.py` routes its forward on), `nevresim_exec_policy(NevresimExecParams) -> ExecPolicySpec` (the **nevresim C++ codegen** firing × sync → `(ComputePolicy, Execution)` choice — `code_generation/generate_main.py:resolve_exec_policy` delegates here; the comparator/reset strings in `NevresimExecParams` are still selected by the codegen helpers, never here; the sync-cycle policy raises since nevresim never runs synchronized), `single_step_activation_replacement` (V5: whether one activation-replacement step subsumes the clamp/shift/quant chain — LIF + TTFS-cycle families; the step planner reads it via `DeploymentPlan.is_lif_style`), `does_conversion_health_calibration` (E3: whether this (firing × sync) cell runs the conversion-health calibration steps — `True` only for `TtfsCascadeModePolicy`, the key the `CalibrationPipeline` reads), and the **backend-capability SSOT** — `supports_backend(backend)` / `require_backend_supported(backend, context)` (single-backend gates) + `valid_backends(candidates)` (subset filter), all delegating to `spiking_semantics` as internals. `policy_for_spiking_mode(mode, schedule)` (= `from_contract`) is the SSOT dispatch; callers take the resolved policy instead of re-branching on `cascaded`/`synchronized` or calling `supports_spiking_mode`/`require_spiking_mode_supported` directly. Mirrors `spiking/segment_policies.py`. |
| `spiking_semantics.py` | `_BACKEND_CAPS`, `supports_spiking_mode`, `require_spiking_mode_supported`, spiking-mode taxonomy | **Per-backend capability matrix** + spiking-mode taxonomy (analytical/cycle/TTFS-family predicates). `_BACKEND_CAPS` declares which backends support which spiking modes. The capability functions are now the *internals* of `SpikingModePolicy` — call-sites (`behavior_config`, `sanafe/runner/core`, `backend`, `pipeline_helpers`) route through the policy rather than calling these directly. |
| `backend.py` | `Backend`, `SimulationBackend`, `BackendRegistry`, `BACKEND_REGISTRY` | **Backend interface + capability-validated registry** (Vector V3). `Backend.supports(contract)` / `require_supported(...)` consult the `_BACKEND_CAPS` matrix via `SpikingModePolicy.supports_backend` / `require_backend_supported` (V2: the policy is the capability SSOT); `SimulationBackend` keeps the existing simulation step classes (nevresim/loihi/sanafe) as the build+run+parity impls and carries each backend's `enable_*` predicate. `BACKEND_REGISTRY.selected_step_specs(plan)` validates every *enabled* backend×mode UP-FRONT at pipeline assembly (raising an actionable error before any step is appended) and returns its step specs — replacing the former informational matrix + reactive per-step validation. Lazy registry proxy avoids the chip_simulation←pipelining import cycle. |
| `certification.py` | `CertificationCell`, `RegressionFloor`, `CertificationFloorBook`, `CertificationStatus`, `CertificationVerdict`, `AbsoluteVerdict`, `certify`, `freeze_cell`, `floor_is_stale`, `load_floor_book`, `save_floor_book`, `FLOOR_BOOK_FORMAT_VERSION` | **Frontier E6 — the certification protocol that REPLACES byte-identity as the Fix-B gate** (pure data + freeze/gate functions; runs nothing). `CertificationCell` is the `(firing × sync × backend)` coordinate, canonical key `mode[/schedule]@backend` (reuses the E3/E4 mode naming; `from_mode_policy` builds it from a `SpikingModePolicy`), plus an optional `variant` discriminator (`mode[/schedule]@backend#variant`, default `None` ⇒ byte-identical key) that keeps distinct floors for deployment configs sharing one recipe cell (e.g. plain vs pruned LIF — the Phase-2 campaign proved one (firing × sync) cell has multiple floors). `RegressionFloor` is the frozen `{deployed_accuracy, wall_clock_s}` + per-cell `eps` / `wall_clock_slack` / `wall_clock_budget_s` + provenance; `CertificationFloorBook` is the JSON-serializable book (the frozen FORMAT, `format_version`-guarded). `freeze_cell` records the CURRENT numbers immutably (`scripts/freeze_certification_floor.py` is the CLI); `certify` is the GATE — PASS iff `deployed_accuracy >= floor − eps` AND `wall_clock_s <= budget`, FAIL naming the regressing side, MISSING_FLOOR (never a silent pass) for an unfrozen cell. **F1 absolute-AC overlay** (additive, byte-identical default): `RegressionFloor` carries optional `ac1_target` / `ac2_reference` (ANN ref, lossless) / `ac5_budget_s`, all default `None` ⇒ the floor round-trips byte-identically (`to_dict` omits unset overlay fields) and the relative verdict is UNCHANGED; `certify` reports an `AbsoluteVerdict` ALONGSIDE the relative status (`ac1_ok` / `ac2_ok` / `ac5_ok` — each `None` when its target is unset; `accuracy_gap_pp` = (deployed − ac1_target) in pp; `ac5_gap_s` = (`max_ft_pass_wall_s` − ac5_budget_s)) — orthogonal, it NEVER changes PASS/FAIL. `floor_is_stale(floor, controller_commit)` is the Q6 warn-capable provenance check (True when the controller commit moved since the floor froze, or no commit was recorded; never hard-fails). Byte-identical: nothing reads the floor until Fix B wires `certify` into the deployment gate. The floor is populated by a GPU matrix run BEFORE the first Fix-B flip (NOT this module). See `docs/CERTIFICATION_PROTOCOL.md`. Enforced by `tests/unit/chip_simulation/test_certification.py`. |
| `cost_extraction.py` | `CostRecord`, `CostScatter`, `extract_cost_record`, `extract_cost_record_from_run`, `load_cost_record`, `save_cost_record`, `weight_reuse_mj`, `COST_RECORD_FORMAT_VERSION`, `COST_RECORD_FILENAME`, `FT_PASS_WALLS_FILENAME` | **Frontier EW3 / R3 — the standing cost-extractor** (pure read, byte-identical: it changes NO sim behavior, only projects numbers the SANA-FE step report / deployed metric already carry). Promotes R3's ad-hoc `mine_artifacts.py` into STANDING infra: on any sim run it emits a `CostRecord` `{acc_deploy, mj_per_sample, spikes, latency_steps, cores, mode, s_global, depth}` keyed to the E6 `CertificationCell` (`mode[/schedule]@backend`), with `energy_proxy_neuron_steps = Σ_d neurons_d · S_d` so the cost model (`energy ∝` that sum, soma-dominated) is reproducible off the record alone. The record also carries the **AC5 per-fine-tuning-PASS wall**: `max_ft_pass_wall_s` (the worst single fine-tuning pass, in seconds — AC5 is judged PER fine-tuning pass, NOT on the end-to-end pipeline wall which is dominated by the non-FT Soft-Core-Mapping / Weight-Quantization / Simulation steps) + the `ft_pass_walls` per-pass breakdown (`({label, wall_s}, …)`), 0.0 / `()` when a run surfaced no FT timing. `extract_cost_record(cell, deployed_accuracy, sanafe_snapshot, max_ft_pass_wall_s=…, ft_pass_walls=…)` reads the `SanafeStepReport.to_snapshot_dict()` projection; `extract_cost_record_from_run(run_dir)` mines a generated-run directory's persisted artifacts (`__target_metric.json` + `_RUN_CONFIG/config.json` + `_GUI_STATE/steps.json` + the AC5 `ft_pass_walls.json` = `FT_PASS_WALLS_FILENAME`); `save_cost_record` writes `cost_record.json` alongside the run. `CostScatter` is the reader/aggregator — the accuracy×cost Pareto data infra keyed to cells: `from_runs`, `by_cell` / `for_cell`, and `pareto_front(cell=…)` (the accuracy↑ × cost↓ non-dominated set, per-cell or cross-cell). The record also carries the **weight-reuse scheduling classification** (round-1 GAP-R, default-off): `reprogram_passes` (N, weight-distinct passes that reload params), `reuse_passes` (M, the cheap passes time-multiplexed through a resident bank), `params_reloaded`, `activation_bytes_moved` (all default 0 ⇒ byte-identical) + the derived `total_passes` / `sync_barrier_count` (= `total_passes − 1`) / `reuse_fraction`; `reuse_mj(mj_per_reprogram=…, mj_per_sync=…)` (= `weight_reuse_mj`, both coefficients default 0.0 ⇒ `0.0`) is the GAP-R mJ term that finally makes a reuse phase cheaper than a reprogram phase; its two opaque coefficients are made DEFENSIBLE (decomposed + uncertainty-banded) in `weight_reuse_cost_model.py`. **P2 wiring:** `reuse_mj_band(coeffs=DEFAULT_COEFFICIENT_BAND) → (low, nominal, high)` is the GROUNDED banded mJ sourced from `weight_reuse_cost_model.phase_cost_band` over the record's phase plan (the opaque `reuse_mj` stays byte-identical); `extract_cost_record(…, coefficient_band=None)` is the production cost-emit site — supplying a `CoefficientBand` stamps the grounded range into `provenance['reuse_cost_band_mj'] = [low, nominal, high]` (comparable per backend), while the default `None` attaches NOTHING ⇒ byte-identical. `backend` is a first-class cost coordinate: `CostRecord.backend` + `CostRecord.cell` carry it (via the `mode[/schedule]@backend` key), so two records of the same recipe on different backends are DISTINCT cells. Format-version (now `3`, bumped for the weight-reuse phase fields) + field-drift guarded. Enforced by `tests/unit/chip_simulation/test_cost_extraction.py`. |
| `weight_reuse_cost_model.py` | `DmaCostCoefficients`, `CoefficientBand`, `PhaseCostBreakdown`, `CostBand`, `Vgg16ScheduledCost`, `DEFAULT_COEFFICIENT_BAND`, `phase_cost_model`, `phase_cost_band`, `vgg16_224_scheduled_cost` | **GAP-R P2 — the DEFENSIBLE per-phase weight-reuse cost model + uncertainty band** (pure data, runs nothing, opt-in; default-off `weight_reuse_mj` path is untouched / byte-identical). Fixes the empty cost column the round-1 default-0 coefficients left: it DECOMPOSES the opaque `mj_per_reprogram` / `mj_per_sync` into NAMED physical units (`DmaCostCoefficients`: `e_dma_per_byte_mj`, `bytes_per_param`, `e_sync_barrier_mj`) each with a CITED literature range, and carries a low/nominal/high `CoefficientBand` so a cost output is a RANGE not a false-precision point. Per-phase model: `reprogram_phase = params_reloaded·bytes_per_param·E_dma + N·E_sync`; `reuse_phase = activation_bytes_moved·E_dma + M·E_sync` (zero coefficients ⇒ 0.0). `phase_cost_model(...) → PhaseCostBreakdown` (reprogram/reuse DMA + sync parts + total + the reprogram-vs-reuse split); `phase_cost_band(...) → CostBand` (low/nominal/high mJ + the nominal breakdown). `DEFAULT_COEFFICIENT_BAND` cites E_dma_per_byte = 31/160/320 pJ/byte (HBM2 / DDR3 / Horowitz-2014 45 nm off-chip DRAM), bytes_per_param = 0.5/1/2 (4/8/16-bit), E_sync_barrier = 0.1/1/10 µJ (Loihi-style NoC flush). `vgg16_224_scheduled_cost() → Vgg16ScheduledCost` is the headline measurement-with-assumptions (N=16 reprogram + M=142 reuse, params_reloaded=66.6M ⇒ nominal 13.4 mJ, band 1.5–49.3 mJ; weight DMA dominates 79.8%; ~6900× reprogram saving vs the naive reprogram-every-position baseline) carrying a `stated_model` string. Doc: `docs/research/GAPR_P2_DEFENSIBLE_COST_MODEL.md`. Enforced by `tests/unit/chip_simulation/test_weight_reuse_cost_model.py`. |
| `coverage_ledger.py` | `AXES`, `AXIS_WILDCARD`, `HypervolumeAxis`, `AxisKind`, `HypervolumeCell`, `CoverageStatus`, `CoverageReport`, `coverage_report`, `claimed_subproduct`, `cell_covers`, `classify_validity_tier`, `row_to_cell`, `row_to_cells`, `collapse_orthogonal_axes`, `active_axes` | **Frontier E1 — the hypervolume coverage ledger** (pure data + a ledger reader; runs nothing): genericity as a MEASURED fraction, not an assertion. `AXES` is the typed product of deployment axes (`firing × sync × encoding_placement × quantization × pruning × backend × mapping_strategy × S × depth × vehicle × dataset × regime`), each `HypervolumeAxis` classified `AxisKind.ORTHOGONAL` (covered MARGINALLY) vs `INTERACTING` (tested JOINTLY) with a cheap-screening `justification`: `firing × sync` and `quantization × firing` are INTERACTING (the death-cascade is a firing×sync law); `depth` is INTERACTING and is a CELL COORDINATE (the death-cascade is depth-driven — deep_mlp's cascaded gap widens d4→d8 — so a shallow INVALID and a deeper VALID_FLAGGED at one recipe must NOT collapse and mutually demote; extracted via the row's `depth` field or a `_d<N>_` run_id token, else `AXIS_WILDCARD`); `encoding_placement` is ORTHOGONAL and **COLLAPSED** (the checkpoint's cheap screen found offload≡subsume under signed-IF, so it is dropped to one representative and is NOT a cell coordinate — `collapse_orthogonal_axes`). `HypervolumeCell` is the FULL-TUPLE cell key that EXTENDS the E6 `CertificationCell` (its `cert_cell` is the embedded `(firing × sync × backend)` sub-coordinate; key = `<mode[/schedule]@backend>|axis=value|…`) so each science-valid ledger row maps to exactly one cell. `classify_validity_tier` normalizes the ledger's free-form `deployment_validity` strings (`VALID_on_chip_majority`, `VALID_FLAGGED_placement`, `INVALID_host_majority`) to the canonical `CoverageStatus` tier; `row_to_cells` maps a row to its cell(s) (a dual-schedule `arch_dataset` row expands to both sync cells). `AXIS_WILDCARD` (`"any"`) is the SSOT default both `row_to_cells` records for an axis a row omits AND `claimed_subproduct` gives an UNPINNED axis (S/depth); `cell_covers(claimed, covered)` matches a claimed wildcard coordinate against ANY covered value, so an S/depth-agnostic claim is covered by a recipe tested at any concrete S/depth (what makes a real `--vehicle deep_cnn` claim score non-zero against rows carrying `S=4`/`depth=8`). `coverage_report(rows, claimed_subproduct=None)` is the GROUP BY: per claimed cell a status in {VALID, VALID_FLAGGED, INVALID, UNTESTED} (worst tier wins, wildcard-aware), the measured `coverage_fraction` (covered / claimed), the named `untested_frontier`, the `research_gap_frontier` (the union of `research_gap_ops` over the FINAL VALID_FLAGGED cells — the future-conversion targets) and the parallel `placement_fixable_frontier` (offloadable encoders, NOT research gaps). CLI: `scripts/campaign/coverage_report.py`. Enforced by `tests/unit/chip_simulation/test_coverage_ledger.py`. |
| `cost_extraction.py` | `CostRecord`, `CostScatter`, `extract_cost_record`, `extract_cost_record_from_run`, `load_cost_record`, `save_cost_record`, `weight_reuse_mj`, `COST_RECORD_FORMAT_VERSION`, `COST_RECORD_FILENAME`, `FT_PASS_WALLS_FILENAME` | **Frontier EW3 / R3 — the standing cost-extractor** (pure read, byte-identical: it changes NO sim behavior, only projects numbers the SANA-FE step report / deployed metric already carry). Promotes R3's ad-hoc `mine_artifacts.py` into STANDING infra: on any sim run it emits a `CostRecord` `{acc_deploy, mj_per_sample, spikes, latency_steps, cores, mode, s_global, depth}` keyed to the E6 `CertificationCell` (`mode[/schedule]@backend`), with `energy_proxy_neuron_steps = Σ_d neurons_d · S_d` so the cost model (`energy ∝` that sum, soma-dominated) is reproducible off the record alone. The record also carries the **AC5 per-fine-tuning-PASS wall**: `max_ft_pass_wall_s` (the worst single fine-tuning pass, in seconds — AC5 is judged PER fine-tuning pass, NOT on the end-to-end pipeline wall which is dominated by the non-FT Soft-Core-Mapping / Weight-Quantization / Simulation steps) + the `ft_pass_walls` per-pass breakdown (`({label, wall_s}, …)`), 0.0 / `()` when a run surfaced no FT timing. `extract_cost_record(cell, deployed_accuracy, sanafe_snapshot, max_ft_pass_wall_s=…, ft_pass_walls=…)` reads the `SanafeStepReport.to_snapshot_dict()` projection; `extract_cost_record_from_run(run_dir)` mines a generated-run directory's persisted artifacts (`__target_metric.json` + `_RUN_CONFIG/config.json` + `_GUI_STATE/steps.json` + the AC5 `ft_pass_walls.json` = `FT_PASS_WALLS_FILENAME`); `save_cost_record` writes `cost_record.json` alongside the run. `CostScatter` is the reader/aggregator — the accuracy×cost Pareto data infra keyed to cells: `from_runs`, `by_cell` / `for_cell`, and `pareto_front(cell=…)` (the accuracy↑ × cost↓ non-dominated set, per-cell or cross-cell). The record also carries the **weight-reuse scheduling classification** (round-1 GAP-R, default-off): `reprogram_passes` (N, weight-distinct passes that reload params), `reuse_passes` (M, the cheap passes time-multiplexed through a resident bank), `params_reloaded`, `activation_bytes_moved` (all default 0 ⇒ byte-identical) + the derived `total_passes` / `sync_barrier_count` (= `total_passes − 1`) / `reuse_fraction`; `reuse_mj(mj_per_reprogram=…, mj_per_sync=…)` (= `weight_reuse_mj`, both coefficients default 0.0 ⇒ `0.0`) is the GAP-R mJ term that finally makes a reuse phase cheaper than a reprogram phase. **P2 wiring:** `reuse_mj_band(coeffs=DEFAULT_COEFFICIENT_BAND) → (low, nominal, high)` is the GROUNDED banded mJ from `weight_reuse_cost_model.phase_cost_band`; `extract_cost_record(…, coefficient_band=None)` (the emit site) stamps the range into `provenance['reuse_cost_band_mj']` ONLY when a band is supplied (default `None` ⇒ byte-identical). `backend` is a first-class cost coordinate (`CostRecord.backend` / `.cell`). Format-version (now `3`, bumped for the weight-reuse phase fields) + field-drift guarded. Enforced by `tests/unit/chip_simulation/test_cost_extraction.py`. |
| `coverage_ledger.py` | `AXES`, `AXIS_WILDCARD`, `HypervolumeAxis`, `AxisKind`, `ScreeningStatus`, `AttributionFidelity`, `HypervolumeCell`, `CoverageStatus`, `FlagMetadata`, `KNOWN_CRACKED_REGIONS`, `CoverageReport`, `coverage_report`, `claimed_subproduct`, `honest_claimed_subproduct`, `cell_covers`, `classify_validity_tier`, `row_to_cell`, `row_to_cells`, `collapse_orthogonal_axes`, `interacting_axes`, `active_axes` | **Frontier E1 + P1 — the SELF-AUDITING hypervolume coverage ledger** (pure data + a ledger reader; runs nothing): genericity as a MEASURED fraction whose DENOMINATOR is a function of each axis's SCREENING STATUS so collapse-on-a-hunch is structurally impossible. `AXES` is the typed product of deployment axes (`firing × sync × encoding_placement × quantization × pruning × backend × mapping_strategy × S × depth × vehicle × dataset × regime`); each `HypervolumeAxis` carries a **`ScreeningStatus`** in {`SCREENED_COLLAPSED`, `ENUMERATED_INTERACTING`, `ASSERTED_UNSCREENED`}. **A `SCREENED_COLLAPSED` axis REQUIRES a non-empty `screening_artifact`** — constructing one without an artifact RAISES (`__post_init__`); `collapsed` is DERIVED (`screening_status is SCREENED_COLLAPSED`). Collapse CONSUMES the status: `collapse_orthogonal_axes` / `interacting_axes` drop ONLY `SCREENED_COLLAPSED` axes; `ENUMERATED_INTERACTING` and `ASSERTED_UNSCREENED` are BOTH counted interacting (enumerated). Reclassification: `encoding_placement` → `SCREENED_COLLAPSED` (artifact = offload==subsume ~1e-6 under signed-IF); `firing`/`sync`/`quantization`/`S`/`depth` → `ENUMERATED_INTERACTING`; `vehicle`/`dataset` → `ENUMERATED_INTERACTING` (artifact = the dual-axis depth×dataset law + architecture-dependent onset — they PROVE interaction); `pruning`/`mapping_strategy`/`backend`/`regime` → `ASSERTED_UNSCREENED` (no screen yet → counted interacting until P3). `honest_claimed_subproduct(**pins)` is the HONEST denominator: every UNPINNED non-collapsed axis is ENUMERATED over its full domain (a `SCREENED_COLLAPSED` axis contributes one representative; `S`/`depth` stay wildcard), so no axis inflates coverage without a linked artifact (deep_cnn legacy 0.60/10 → honest 0.0023/2560 firing-pinned, 0.000625/12800 full). `claimed_subproduct` is the LEGACY single-default claim. `HypervolumeCell` EXTENDS the E6 `CertificationCell` (`cert_cell` = `(firing × sync × backend)`; key = `<mode[/schedule]@backend>|axis=value|…`). `classify_validity_tier` normalizes free-form `deployment_validity` strings to a `CoverageStatus` tier; `row_to_cells` maps a row to its cell(s) (a dual-schedule `arch_dataset` row → both sync cells). `coverage_report(rows, claimed_subproduct=None, now_ts=None)` is the GROUP BY: per claimed cell a status in {VALID, VALID_FLAGGED, INVALID, UNTESTED} (worst tier wins, wildcard-aware), the measured `coverage_fraction` + first-class `claimed_subproduct_size` (ALWAYS printed next to the fraction), the VALID and VALID_FLAGGED tier counts kept SEPARATE (never merged — `to_dict` emits no `valid_total`), the per-region `attribution_fidelity` (`KNOWN_CRACKED_REGIONS` → `VALUE_DOMAIN_ONLY`: GAP-1 coalescing+neuron_split at VGG scale + the residual Tier-1 merge), the `flag_metadata` (`FlagMetadata` owner + `age_days` vs `now_ts`; the live ledger's epoch-float `ts` is parsed), the `research_gap_frontier` and `placement_fixable_frontier`. CLI: `scripts/campaign/coverage_report.py` (`--ci`, `--legacy-denominator`, `--now`). Doc: `docs/research/HYPERVOLUME.md`. Enforced by `tests/unit/chip_simulation/test_coverage_ledger.py` + `test_coverage_self_audit.py`. |
| `coverage_ci.py` | `CoverageGuardError`, `DEFAULT_FLAG_AGE_DAYS`, `assert_axes_screening_sound`, `assert_no_merged_valid_tiers`, `assert_no_aged_unowned_flags`, `audit_coverage_instrument` | **Frontier P1 CI guard** for the self-auditing coverage instrument (pure checks, raise on violation). `assert_axes_screening_sound` FAILS if any axis is `SCREENED_COLLAPSED` without a `screening_artifact` (defense-in-depth past the dataclass `__post_init__`, catching a status tampered after construction); `assert_no_merged_valid_tiers` FAILS if a report dict carries a headline that fuses VALID + VALID_FLAGGED (`valid_total` / `covered_valid_total` / …) — a flagged cell is never claimed plainly valid; `assert_no_aged_unowned_flags` FAILS if a flag is older than `max_age_days` (default 90) AND has no owner (a flag must not rot silently). `audit_coverage_instrument(axes, report)` runs all three. Enforced by `tests/unit/chip_simulation/test_coverage_self_audit.py`. |
| `deployment_faithfulness.py` | `DEPLOYMENT_FAITHFULNESS_GATES`, `standing_gates`, `EXTERNAL_DEPENDENCY_BOUNDARIES`, `boundary_for`, `assert_sanafe_pin_consistent`, `bootstrap_pinned_sanafe_version`, `sanafe_supported_versions`, `DEPLOYED_METRIC_PROTOCOL` | **R6 / Frontier E5 — deployment-faithfulness SSOT** (pure data, no execution). Declares the STANDING per-run gates (`scm_torch_sim_parity_check`, `nf_scm_parity_samples`; both default-on, wired in `SoftCoreMappingStep`) so the deployed-forward number stays the only number of record; the audit registry of every external-dependency integration boundary (`sanafe` version-pin + capability-gate + lazy-import; `lava` capability-gate + lazy-import; `ffcv` lazy-import) with a `verify()` that confirms the guard is live; and **drift detection** — `assert_sanafe_pin_consistent` cross-checks the `bootstrap_sanafe.sh` `pip install sanafe==X` literal against the code guard's `_SUPPORTED_SANAFE_VERSIONS` SSOT (a one-sided bump or silent upgrade fails loud, the SANA-FE 2.2.x-SIGFPE lesson). `DEPLOYED_METRIC_PROTOCOL` pins which entry points constitute the deployed number so a metric-protocol rewire fails loud. Enforced by `tests/unit/chip_simulation/test_deployment_faithfulness.py`. |
| `spike_modes.py` | `to_spikes`, stochastic/deterministic helpers | Single torch spike-encoding implementation for unified and hybrid core flows. |
| `spike_recorder.py` | `RunRecord`, `SegmentSpikeRecord`, `CoreSpikeCounts`, `compare_records` | HCM/Loihi spike-count recording and diff utilities (segment inputs, per-core in/out, segment outputs). |
| `lava_loihi/runner.py` | `LavaLoihiRunner` | Optional Lava Loihi LIF runner. Accepts `NeuralBehaviorConfig`; **`run_segments_from_reference()`** — production HCM-vs-Lava parity. |
| `subtractive_lif.py` | `SubtractiveLIFReset` | Lava LIF process: configurable reset (`zero_reset`), thresholding, active-window gating. |
| `_spike_encoding.py` | `encode_segment_input`, `uniform_rate_encode`, … | Shared numpy batch encoder for Lava/SANA-FE segment injection; Uniform uses torch parity path. |
| `sanafe/` (sub-package) | `SanafeRunner`, `SanafeRunRecord`, … | Optional SANA-FE detailed-stats + parity. See `sanafe/ARCHITECTURE.md`. |

### Cross-backend numeric alignment

| Path | Dtype note |
|------|------------|
| HCM (`SpikingHybridCoreFlow`) | `torch.float64` default for membrane/TTFS math; weights float32 Parameters |
| TTFS reference | `TtfsAnalyticalExecutor` + `ttfs_segment.py` — single numpy semantics for HCM, Nevresim (raw activations), and SANA-FE buffer propagation |
| SANA-FE TTFS | Plugins + `TtfsAnalyticalExecutor` contract; hardware parity via `potential_trace`; no `sana_fe` core forks |
| Loihi/Lava | LIF only — TTFS does not map onto Loihi LIF dynamics; pipeline rejects `enable_loihi_simulation` + TTFS |
| SANA-FE (`SanafeRunner`) | `numpy.float64` segment assembly; float32 boundary drift can change ±1 spike at rate-encoding edges |
| nevresim TTFS | C++ `signal_t = double`; int rate-coded path uses exact integer arithmetic |
| Loihi parity | HCM `forward_with_recording` reference; per-core counts only at `B=1` |

All three deployment simulators depend on correct **`ChipLatency`** scheduling (see `mapping/ARCHITECTURE.md` § ChipLatency pitfalls) before parity gates are meaningful.

## Dependencies

- **Internal**: `code_generation`, `mapping`, `common`, `data_handling`.
- **External**: `subprocess`, `numpy`, `torch`; optional `lava`, `sanafe`, `ffcv` (runtime only). Each optional dependency is a declared integration boundary in `deployment_faithfulness.EXTERNAL_DEPENDENCY_BOUNDARIES` with a version-pin and/or capability-gate and/or lazy-import guard; the audit checklist (`test_deployment_faithfulness.py`) fails loud if a boundary lacks a guard.

## Dependents

- `pipelining.pipeline_steps.simulation_step` — `SimulationRunner` / nevresim.
- `pipelining.pipeline_steps.loihi_simulation_step` — `record_hcm_reference` + `LavaLoihiRunner` + `assert_spike_parity_or_raise`.
- `pipelining.pipeline_steps.sanafe_simulation_step` — `SanafeRunner` + optional HCM parity via `simulation_factory`.
- `pipelining.simulation_factory` — shared HCM build and metric test for SCM/HCM.
- `pipelining.core.pipelines.deployment_specs` — `BACKEND_REGISTRY.selected_step_specs(plan)` selects/validates backend steps at assembly.
- Entry point sets `NevresimDriver.nevresim_path`.

## Exported API (`__init__.py`)

`spike_modes`, `Backend`, `SimulationBackend`, `BackendRegistry`, `BACKEND_REGISTRY`, the
E6 certification API (`CertificationCell`, `RegressionFloor`, `CertificationFloorBook`,
`CertificationStatus`, `CertificationVerdict`, `certify`, `freeze_cell`, `load_floor_book`,
`save_floor_book`), the EW3 cost-extractor API (`CostRecord`, `CostScatter`,
`extract_cost_record`, `extract_cost_record_from_run`, `load_cost_record`, `save_cost_record`),
the GAP-R P2 defensible-cost-model API (`DmaCostCoefficients`, `CoefficientBand`, `CostBand`,
`PhaseCostBreakdown`, `DEFAULT_COEFFICIENT_BAND`, `phase_cost_model`, `phase_cost_band`,
`vgg16_224_scheduled_cost`), and the E1 coverage-ledger API (`AXES`, `HypervolumeAxis`,
`HypervolumeCell`, `CoverageStatus`, `CoverageReport`, `coverage_report`, `claimed_subproduct`).
the E1+P1 coverage-ledger API (`AXES`, `HypervolumeAxis`, `HypervolumeCell`, `ScreeningStatus`,
`AttributionFidelity`, `FlagMetadata`, `CoverageStatus`, `CoverageReport`, `coverage_report`,
`claimed_subproduct`, `honest_claimed_subproduct`, `interacting_axes`), and the P1 coverage-CI
guards (`CoverageGuardError`, `assert_axes_screening_sound`, `assert_no_merged_valid_tiers`,
`assert_no_aged_unowned_flags`, `audit_coverage_instrument`).
